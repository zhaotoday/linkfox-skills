#!/usr/bin/env python3
"""bg.promotion.activity.candidate.goods.query — 活动候选商品查询."""
import _eu_promotion_script as m

if __name__ == "__main__":
    m.run_cli(
        "bg.promotion.activity.candidate.goods.query",
        "eu_promotion_activity_candidate_goods_query.py '<JSON>'",
    )
